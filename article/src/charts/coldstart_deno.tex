
\begin{figure}
    \begin{tikzpicture}
        \begin{axis} [
                width=\linewidth,
                height=0.4\linewidth,
                legend style={
                        at={(0,1.025)},
                        anchor=south west,
                        font=\tiny
                    },
                legend cell align={left},
                legend columns=4,
                grid,
                grid style=dashed,
                xlabel={nth run},
                ylabel={Time normalized},
                mark options={solid},
                xtick distance=1,
                ytick distance=0.25,
                ymin=0,
                xmin=-0.5,
                xmax=8.5,
                cycle list/Set1
            ]

            % Sequential

            \plotColdstart{coldstart/js-sequential_coldstart_SHT_Simple_deno.csv}{}
            \addlegendentry{JS Seq.}
            \pgfplotsset{cycle list shift=-1}
            \plotColdstart{coldstart/js-sequential_coldstart_SHT_Simple_Lookup_deno.csv}{dashed}
            \addlegendentry{JS Seq. (LUT)}

            % Workers

            \plotColdstart{coldstart/js-workers_coldstart_SHT_Simple_deno.csv}{{}}
            \addlegendentry{Workers}
            \pgfplotsset{cycle list shift=-2}
            \plotColdstart{coldstart/js-workers_coldstart_SHT_Simple_Lookup_deno.csv}{dashed}
            \addlegendentry{Workers (LUT)}

        \end{axis}
    \end{tikzpicture}

    \label{plot:coldstart-deno}
    \caption{Normalized coldstart execution times for methods implemented in Deno environment.}
\end{figure}
